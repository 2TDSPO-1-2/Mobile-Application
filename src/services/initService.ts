/**
 * Inicialização do app — sem seed automático.
 * Dados de negócio vêm da API Oracle via backend.
 * Para dados offline de desenvolvimento, use loadDevSeed() em src/data/devSeed.ts.
 */
export async function initializeAppData(): Promise<void> {
  return;
}
